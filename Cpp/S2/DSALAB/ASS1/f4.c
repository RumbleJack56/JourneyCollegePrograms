#include<stdio.h>
#include<stdlib.h>
typedef struct Node{
  int val;
  Node* next;
};

int main(){
  int n;
  printf("Welcome to linked list inputter, Enter number of elements to enter: ")
  
  Node* head = (Node*)malloc(sizeof(Node));
  
}